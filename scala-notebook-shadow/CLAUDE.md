The goal of this repository is to provide a VS Code extension that generates shadow files for Scala Notebook scripts, enabling better integration with the Metals language server and improving the development experience for notebooks in a mill / metals repository.

The extension watches for changes in Scala Notebook scripts and regenerates corresponding shadow files, which can then be compiled and analyzed by Metals.

Use the `cellar` CLI to discover the api of external jvm artefacts.